---
title: Button
description: Displays a button or a component that looks like a button.
dependencies: ['@radix-ui/react-slot']
---

<ComponentPreview name='button'>
```json doc-gen:file
{
  "file": "./src/components/demos/Button/Button.tsx",
  "codeblock": true
}
```
</ComponentPreview>

## Installation

<Steps>

  <Step>
    ### Install the primitive
    
    Install the `@radix-ui/react-slot` package.

    ```package-install
    @radix-ui/react-slot
    ```

  </Step>
    
  <Step>
    ### Copy-paste the component
    
    Copy and paste the component code in a `.tsx` file.

    ```json doc-gen:file
    {
      "file": "../../packages/ui/src/button.tsx",
      "codeblock": true
    }
    ```

  </Step>

  <Step>
    ### Update import paths

    Update the `@kosori/ui` import paths to fit your project structure, for example, using `~/components/ui`.

  </Step>

</Steps>

## Usage

```ts
import { Button } from '~/components/ui/button';
```

```tsx
<Button variant='outline'>Button</Button>
```

## Reference

<TypeTable
  type={{
    variant: {
      default: 'solid',
      description: 'The visual style of the button',
      type: 'enum',
      typeDescription: "'solid' | 'soft' | 'outline' | 'ghost'",
    },
    intent: {
      default: 'default',
      description: 'The intent of the button',
      type: 'enum',
      typeDescription: "'default' | 'error'",
    },
    size: {
      default: 'medium',
      description: 'The size of the button',
      type: 'enum',
      typeDescription: "'small' | 'medium' | 'large'",
    },
    icon: {
      default: 'false',
      description: 'If true, adjusts the button size for icon-only usage',
      type: 'boolean',
    },
  }}
/>

## Link

You can use the `buttonStyles` helper to create a link that looks like a button.

```ts
import { buttonVariants } from '~/components/ui/button';
```

```tsx
<Link className={buttonStyles({ variant: 'outline' })}>Click here</Link>
```

## Examples

### Intents & Variants

<ComponentPreview name='button-intents'>
```json doc-gen:file
{
  "file": "./src/components/demos/Button/ButtonIntents.tsx",
  "codeblock": true
}
```
</ComponentPreview>

### Sizes

<ComponentPreview name='button-sizes'>
```json doc-gen:file
{
  "file": "./src/components/demos/Button/ButtonSizes.tsx",
  "codeblock": true
}
```
</ComponentPreview>

### Icon

<ComponentPreview name='button-icon'>
```json doc-gen:file
{
  "file": "./src/components/demos/Button/ButtonIcon.tsx",
  "codeblock": true
}
```
</ComponentPreview>
