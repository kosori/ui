---
title: Card
description: Displays a card with header, content, and footer.
---

<ComponentPreview name='card'>
```json doc-gen:file
{
  "file": "./src/components/demos/card/card.tsx",
  "codeblock": true
}
```
</ComponentPreview>

## Installation

<InstallTabs items={['CLI', 'Manual']}>

  <Tab value='CLI'>
  
    <KosoriCLITabs>
      ```bash tab="npm"
      npx @kosori/cli add components card
      ```

      ```bash tab="pnpm"
      pnpx @kosori/cli add components card
      ```

      ```bash tab="yarn"
      yarn dlx @kosori/cli add components card
      ```

      ```bash tab="bun"
      bun x @kosori/cli add components card
      ```

    </KosoriCLITabs>

  </Tab>

  <Tab value='Manual'>

    <Steps>

      <Step>
        ### Copy-paste the component

        Copy and paste the component code in a `.tsx` file.

        ```json doc-gen:file
        {
          "file": "../../packages/ui/src/card.tsx",
          "codeblock": true
        }
        ```

      </Step>

      <Step>
        ### Update import paths

        Update the `@kosori/ui` import paths to fit your project structure, for example, using `~/components/ui`.

      </Step>

    </Steps>

  </Tab>

</InstallTabs>

## Usage

```ts
import {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from '~/components/ui/card';
```

```tsx
<Card>
  <CardHeader>
    <CardTitle>Card Title</CardTitle>
    <CardDescription>Card Description</CardDescription>
  </CardHeader>
  <CardContent>
    <p>Card Content</p>
  </CardContent>
  <CardFooter>
    <p>Card Footer</p>
  </CardFooter>
</Card>
```
