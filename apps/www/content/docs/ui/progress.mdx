---
title: Progress
description: Displays an indicator showing the completion progress of a task, typically displayed as a progress bar.
links:
  doc: https://www.radix-ui.com/docs/primitives/components/progress
  api: https://www.radix-ui.com/docs/primitives/components/progress#api-reference
dependencies: ['@radix-ui/react-progress']
---

<ComponentPreview name='progress'>
```json doc-gen:file
{
  "file": "./src/components/demos/Progress/Progress.tsx",
  "codeblock": true
}
```
</ComponentPreview>

## Installation

<InstallTabs items={['CLI', 'Manual']}>

  <Tab value='CLI'>
  
    <KosoriCLITabs>
      ```bash tab="npm"
      npx @kosori/cli add components progress
      ```

      ```bash tab="pnpm"
      pnpx @kosori/cli add components progress
      ```

      ```bash tab="yarn"
      yarn dlx @kosori/cli add components progress
      ```

      ```bash tab="bun"
      bun x @kosori/cli add components progress
      ```

    </KosoriCLITabs>

  </Tab>

  <Tab value='Manual'>

    <Steps>

      <Step>
        ### Install the primitive

        Install the `@radix-ui/react-progress` package.

        ```package-install
        @radix-ui/react-progress
        ```

      </Step>

      <Step>
        ### Copy-paste the component

        Copy and paste the component code in a `.tsx` file.

        ```json doc-gen:file
        {
          "file": "../../packages/ui/src/progress.tsx",
          "codeblock": true
        }
        ```

      </Step>

      <Step>
        ### Update import paths

        Update the `@kosori/ui` import paths to fit your project structure, for example, using `~/components/ui`.

      </Step>

    </Steps>

  </Tab>

</InstallTabs>

## Usage

```ts
import { Progress } from '~/components/ui/progress';
```

```tsx
<Progress value={33} />
```
