---
title: Drawer
description: A drawer component for React.
links:
  doc: https://github.com/emilkowalski/vaul
dependencies: ['vaul']
---

<ComponentPreview name='drawer'>
```json doc-gen:file
{
  "file": "./src/components/demos/drawer/drawer.tsx",
  "codeblock": true
}
```
</ComponentPreview>

## Installation

<InstallTabs items={['CLI', 'Manual']}>

  <Tab value='CLI'>
  
    <KosoriCLITabs>
      ```bash tab="npm"
      npx @kosori/cli add components drawer
      ```

      ```bash tab="pnpm"
      pnpx @kosori/cli add components drawer
      ```

      ```bash tab="yarn"
      yarn dlx @kosori/cli add components drawer
      ```

      ```bash tab="bun"
      bun x @kosori/cli add components drawer
      ```

    </KosoriCLITabs>

  </Tab>

  <Tab value='Manual'>

    <Steps>

      <Step>
        ### Install the primitive

        Install the `vaul` package.

        ```package-install
        vaul
        ```

      </Step>

      <Step>
        ### Copy-paste the component

        Copy and paste the component code in a `.tsx` file.

        ```json doc-gen:file
        {
          "file": "../../packages/ui/src/drawer.tsx",
          "codeblock": true
        }
        ```

      </Step>

      <Step>
        ### Update import paths

        Update the `@kosori/ui` import paths to fit your project structure, for example, using `~/components/ui`.

      </Step>

    </Steps>

  </Tab>

</InstallTabs>

## Usage

```ts
import {
  Drawer,
  DrawerClose,
  DrawerContent,
  DrawerDescription,
  DrawerFooter,
  DrawerHeader,
  DrawerTitle,
  DrawerTrigger,
} from '~/components/ui/drawer';
```

```tsx
<Drawer>
  <DrawerTrigger>Open</DrawerTrigger>
  <DrawerContent>
    <DrawerHeader>
      <DrawerTitle>Are you absolutely sure?</DrawerTitle>
      <DrawerDescription>This action cannot be undone.</DrawerDescription>
    </DrawerHeader>
    <DrawerFooter>
      <Button>Submit</Button>
      <DrawerClose>
        <Button variant='outline'>Cancel</Button>
      </DrawerClose>
    </DrawerFooter>
  </DrawerContent>
</Drawer>
```

## Examples

### Responsive

You can combine the `<Dialog />` and `<Drawer />` components to create a responsive dialog. This renders a `<Dialog />` component on desktop and a `<Drawer />` on mobile.

<ComponentPreview name='drawer-dialog'>
```json doc-gen:file
{
  "file": "./src/components/demos/drawer/drawer-dialog.tsx",
  "codeblock": true
}
```
</ComponentPreview>
